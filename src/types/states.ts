export enum DeployedState {
  Deleted = -1,
  NotDeployed = 0,
  Deployed = 1,
  Failed = 2
}

export enum RunningState {
  Stopped = 0,
  Running = 1,
  Broken = 2
}

export enum ActiveState {
  Inactive = 0,
  Active = 1
}